# cuatrimotosatv.com — sitio compilado

Salida de `npm run build` del proyecto Astro (`Desktop/cuatrimotosatv`).
Este repositorio **no es el código fuente**: es el sitio ya construido, y
existe solo para que Hostinger lo publique (hPanel → Avanzado → GIT).

No edites nada acá. Los cambios se hacen en el proyecto Astro y se publican
con `bash scripts/publicar.sh`, que reconstruye y empuja a `main`.

Incluye `.htaccess` con las 13 redirecciones 301 heredadas del WordPress viejo.
